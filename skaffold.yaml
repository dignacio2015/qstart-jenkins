apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/dignacio2015/qstart-jenkins:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/dignacio2015/qstart-jenkins:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: qstart-jenkins
        chartPath: charts/qstart-jenkins
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/dignacio2015/qstart-jenkins'
          image.tag: '{{.DIGEST_HEX}}'
